# {{projectName}}

{{projectDescription}}

## Commands

```bash
# 安装依赖
pnpm install

# 开发
pnpm dev

# 构建
pnpm build

# 类型检查
pnpm typecheck
```

## Architecture

pnpm monorepo 前端项目，基于 Vue 3 + TypeScript。

| 目录 | 说明 |
|---|---|
| apps/web/ | 主应用 |
| packages/ | 共享包（按需添加） |

### apps/web/ 结构

| 目录 | 说明 |
|---|---|
| views/ | 页面组件（一个路由对应一个文件） |
| components/ | 可复用业务组件 |
| composables/ | 组合式函数（use* 开头） |
| layouts/ | 布局组件 |
| stores/ | Pinia 状态管理 |
| router/ | Vue Router 配置 |
| api/ | API 请求模块 |
| types/ | 共享类型定义 |

## Git Commit Scope

本项目的 scope 枚举：`web` | `shared`
