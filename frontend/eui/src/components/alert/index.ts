/**
 * EAlert 警告提示组件的导出入口
 * 对外暴露组件、样式变体配置及类型定义
 */
export { default as EAlert } from './EAlert.vue'
export { alertVariants } from './types'
export type { EAlertProps, EAlertEmits, AlertVariants } from './types'
