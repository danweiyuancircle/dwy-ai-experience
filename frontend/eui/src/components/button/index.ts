/**
 * EButton 按钮组件的导出入口
 * 对外暴露组件、CVA 样式变体配置及类型定义
 */
export { default as EButton } from './EButton.vue'
export { buttonVariants } from './types'
export type { EButtonProps, ButtonVariants } from './types'
