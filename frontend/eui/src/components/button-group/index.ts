/**
 * EButtonGroup 按钮组组件的导出入口
 * 将多个按钮合并为视觉上相连的一组
 */
export { default as EButtonGroup } from './EButtonGroup.vue'
export { buttonGroupVariants } from './types'
export type { EButtonGroupProps, ButtonGroupVariants } from './types'
