/**
 * EField 表单字段容器组件的导出入口
 * 比 EFormItem 更基础，仅做布局包裹（label/control/description 的排列）
 */
export { default as EField } from './EField.vue'
export { fieldVariants } from './types'
export type { EFieldProps, FieldVariants } from './types'
