/**
 * EFormDialog 表单对话框组件的导出入口
 * 在 EDialog 基础上预置确认/取消按钮与 loading 态，适合弹窗表单场景
 */
export { default as EFormDialog } from './EFormDialog.vue'
export type { EFormDialogProps, EFormDialogEmits } from './types'
